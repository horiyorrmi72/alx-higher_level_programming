#!/usr/bin/python3
def new_in_list(my_list, idx, element):
        if not my_list:
            return
        list_copy = my_list.copy()
        if 0 <= idx < len(new_list):
            list_copy[idx] = element
            return list_copy
